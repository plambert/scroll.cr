# Demo recordings live in LFS: they are rewritten whole on every re-record, and
# a few MB of GIF per release would otherwise stay in history forever.
demo/static/*.gif filter=lfs diff=lfs merge=lfs -text
